import React from 'react';
import './HieSpaceComponent.scss';
const HieSpaceComponent = (props) => {
    return (
        <div className="hie-space" style={{

        }}>

        </div>
    );
}

export default HieSpaceComponent;